import { Box, Container, Typography } from "@mui/material";

const Institute = () => {
    return (
        <Box component="section">
            <Container fixed>
                <Typography variant="h2">Institue</Typography>
            </Container>
        </Box>
    );
}

export default Institute;