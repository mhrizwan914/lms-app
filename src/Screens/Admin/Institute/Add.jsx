import { Box, Container, Typography } from "@mui/material";

const Add = () => {
    return (
        <Box component="section">
            <Container fixed>
                <Typography variant="h2">Add Institue Screen</Typography>
            </Container>
        </Box>
    );
}

export default Add;