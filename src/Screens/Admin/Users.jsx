import { Box, Container, Typography } from "@mui/material";

const Users = () => {
    return (
        <Box component="section">
            <Container fixed>
                <Typography variant="h2">Users Screen</Typography>
            </Container>
        </Box>
    );
}

export default Users;